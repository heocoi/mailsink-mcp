# Changelog

## 0.1.3 — 2026-04-21

- Repository moved to its own public home: [heocoi/mailsink-mcp](https://github.com/heocoi/mailsink-mcp). Source is now browsable instead of living inside a private monorepo. No functional changes.
- `package.json` `repository.url` and `bugs.url` updated to point at the new public repo.

## 0.1.2 — 2026-04-20

- Single `mcp` bin entry so `npx @mailsink/mcp` resolves correctly.

## 0.1.1 — 2026-04-20

- Publish metadata, README, and LICENSE.

## 0.1.0 — 2026-04-20

- Initial release. Eight tools: `create_inbox`, `wait_for_email`, `get_verification_code`, `get_verification_link`, `list_messages`, `list_inboxes`, `get_message`, `delete_inbox`.
